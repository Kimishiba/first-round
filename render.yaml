services:
  - type: web
    name: first-round
    env: node
    region: oregon # Default region, can be changed
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false # Requires manual entry in the Render dashboard
